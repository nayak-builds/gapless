# Gapless frontend

Next.js 14 App Router UI. Tokens live in `tailwind.config.ts`. Shared chrome: `components/layout`. Primitives: `components/ui`.

```powershell
npm install
npm run dev
```

Open http://localhost:3000
